# NameClear

Is your brand name available? NameClear checks a name across five domain extensions, the live
USPTO federal trademark register, and the four major social platforms — in one pass. The instant
check is free and unlimited; the full PDF write-up is a one-time **$9** via Stripe Checkout.

## What it checks

- **Domains** — `.com .co .io .net .app` queried live over RDAP against each registry (Verisign,
  Google, Identity Digital). "Available" means the registry returned 404 for the domain name.
- **Trademarks** — live USPTO TMSearch API, alive records only, with direct links to each official
  record and exact-vs-close classification.
- **Social** — X, Instagram, TikTok, YouTube handle probes with suggested alternates when taken.

## Stack

- React 18 + TypeScript + Vite 6 + Tailwind CSS v4 (`src/app/`)
- Cloudflare Pages Functions backend (`functions/`, shared logic in `server/`)
- Supabase for the 1-hour check cache + paid report records + PDF storage
- Stripe Checkout for the one-time $9 payment
- Resend for emailing reports

## Environment

Copy `.env.example` to `.env.local` (local) and set the same variables in Cloudflare Pages settings
(production + preview). Only `VITE_*` values are public. `STRIPE_SECRET_KEY`, `SUPABASE_SERVICE_ROLE_KEY`,
`RESEND_API_KEY`, and `TMSEARCH_WAF_TOKEN` are server-side secrets used only by the Pages Functions —
never import them from `src/`.

To try it with zero credentials, run the dev stack and use the checker with no env vars set:
domain/social checks work unauthenticated, and trademark returns a graceful "unavailable".

## Run locally

```bash
npm install
npm run dev:full   # builds dist, then runs Vite (5173) + wrangler pages dev (8788) together
```

Open http://localhost:5173 — `/api/*` is proxied to the Functions on 8788. Or run the two pieces
separately: `npm run dev` (frontend) and `npm run api` (functions on 8788).

## Database

`supabase/migrations/20260101000000_init.sql` creates the two tables (`search_cache`,
`paid_reports`) and the private `reports` storage bucket. Apply it to your Supabase project and
expose no public anon access — the backend uses the service-role key server-side only.

## Deployment

```bash
npm run deploy   # typecheck + build + sitemap, then `wrangler pages deploy dist`
```

Set the Stripe webhook (`checkout.session.completed`) to
`https://<project>.pages.dev/api/webhook/stripe` with the `STRIPE_WEBHOOK_SECRET` configured for
async fulfillment. The success page also fulfills on-load as a fallback, so the report is always
delivered even if the webhook hasn't fired yet.

## SEO

- `public/robots.txt` — allows GPTBot, ClaudeBot, PerplexityBot, CCBot
- `public/llms.txt` — plain-text summary for LLMs
- `public/sitemap.xml` — regenerated by `npm run build` (`scripts/generate-sitemap.mjs`)
- JSON-LD: Organization (static, `index.html`) + per-page WebPage/FAQPage/AboutPage (injected)
- One `<h1>` per page; canonical + OG/Twitter tags set at runtime

**Sitemap URL:** printed at the end of every build (`npm run build`).
